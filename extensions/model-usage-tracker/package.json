{
  "name": "@openclaw/model-usage-tracker-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Records per-turn model/token usage to a JSONL log via llm_output + reply_payload_sending hooks",
  "type": "module",
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
